<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.DIVE2025.domain.member.mapper.MemberMapper">

    <!-- username 중복 체크 -->
    <select id="countByUsername" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM shelter
        WHERE username = #{username}
    </select>

    <!-- username으로 보호소 조회 -->
    <select id="findByUsername" parameterType="string" resultType="com.example.DIVE2025.domain.shelter.entity.Shelter">
        SELECT
            id,
            username,
            password,
            description,
            tel,
            addr,
            latitude,
            longitude,
            shelter_feature AS shelterFeature
        FROM shelter
        WHERE username = #{username}
    </select>

    <!-- PK(id)로 보호소 조회 -->
    <select id="findById" parameterType="long" resultType="com.example.DIVE2025.domain.shelter.entity.Shelter">
        SELECT
            id,
            username,
            password,
            description,
            tel,
            addr,
            latitude,
            longitude,
            shelter_feature AS shelterFeature
        FROM shelter
        WHERE id = #{id}
    </select>

    <!-- 신규 보호소 등록 -->
    <insert id="insert" parameterType="com.example.DIVE2025.domain.shelter.entity.Shelter" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO shelter (
            username,
            password,
            description,
            tel,
            addr,
            latitude,
            longitude,
            shelter_feature
        ) VALUES (
                     #{username},
                     #{password},
                     #{description},
                     #{tel},
                     #{addr},
                     #{latitude},
                     #{longitude},
                     #{shelterFeature}
                 )
    </insert>

</mapper>
